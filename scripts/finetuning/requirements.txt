# Fine-tuning dependencies / Dipendenze per il fine-tuning
# Install with / Installa con: pip install -r requirements.txt

# Core ML libraries / Librerie ML principali
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
peft>=0.6.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
trl>=0.7.0
